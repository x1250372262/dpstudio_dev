# $comment

#foreach( $apiAction in $apiActions)

---
# $apiAction.title
> $apiAction.comment
> 地址: $apiAction.uri
#if($apiAction.methods.size() > 0)
> 支持方式: $apiAction.methods
#else
> 支持方式: 所有
#end

**参数:**

参数名|类型|是否必填|描述|示例值
-----|------|------|------
#foreach($p in $apiAction.params)
$p.paramName|$p.paramType|#if($p.require) 是 #else 否 #end|$p.paramDesc|$p.demoValue
#end

**返回:**
$apiAction.returnDesc

参数名|类型|是否必填|描述|示例值|返回key|返回数据类型
-----|------|------|------|------|------
#foreach($p in $apiAction.respParam)
$p.paramName|$p.paramType|#if($p.require) 是 #else 否 #end|$p.paramDesc|$p.demoValue|$p.dataKey|$p.dataType
#end

#if($apiAction.respbody)
返回：
```json
$apiAction.respbody
```
#end
#end